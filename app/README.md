# NovaArcade Apps Showcase

移动端优化的应用展示页面，展示五个 NovaArcade 游戏应用。

## 功能特性

- 📱 **移动端优先设计**：专为手机端优化的响应式布局
- 🌐 **多语言支持**：中英文切换，易于扩展更多语言
- 🖼️ **图片懒加载**：优化性能，减少初始数据加载
- 🎨 **现代化界面**：使用 Tailwind CSS 构建的精致 UI
- ⚡ **高性能**：Vite 构建，代码分割，优化加载速度
- 🔧 **易于维护**：模块化代码结构，应用数据集中管理

## 项目结构

```
app/
├── src/
│   ├── index.html          # 主页面
│   ├── main.js            # 主应用逻辑
│   ├── style.css          # 主样式文件
│   ├── apps-data.js       # 应用数据配置
│   ├── locales/           # 多语言文件
│   │   ├── index.js      # 语言系统
│   │   ├── en.json       # 英文翻译
│   │   └── zh.json       # 中文翻译
│   ├── components/        # 组件目录（预留）
│   └── utils/            # 工具函数目录（预留）
├── public/               # 静态资源
│   └── images/          # 应用截图
├── package.json
├── vite.config.js       # Vite 配置
├── tailwind.config.js   # Tailwind 配置
└── README.md
```

## 开发指南

### 环境要求

- Node.js 16+ 
- npm 或 yarn

### 安装依赖

```bash
cd app
npm install
```

### 开发模式

```bash
npm run dev
```

访问 `http://localhost:3000`

### 构建生产版本

```bash
npm run build
```

构建结果位于 `dist/` 目录

### 预览构建结果

```bash
npm run preview
```

## 部署到 Cloudflare Pages

### 前提条件

1. Cloudflare 账户
2. 项目代码推送到 Git 仓库（GitHub/GitLab）

### 部署步骤

1. **登录 Cloudflare Dashboard**
   - 进入 [Cloudflare Dashboard](https://dash.cloudflare.com)
   - 选择 "Workers & Pages"

2. **创建新项目**
   - 点击 "Create application"
   - 选择 "Pages"
   - 点击 "Connect to Git"

3. **配置 Git 仓库**
   - 选择你的 Git 仓库
   - 点击 "Begin setup"

4. **配置构建设置**
   - **项目名称**: `novaarcade-apps`（或自定义）
   - **生产分支**: `main`
   - **构建命令**: `npm run build`
   - **输出目录**: `dist`
   - **环境变量**: 无需特殊配置

5. **部署**
   - 点击 "Save and Deploy"
   - Cloudflare 会自动构建并部署你的应用

6. **自定义域名（可选）**
   - 部署完成后，进入项目设置
   - 点击 "Custom domains"
   - 添加你的域名并按照指引配置 DNS

### 持续部署

- 每次推送到 `main` 分支会自动触发重新部署
- 每个 Pull Request 会自动创建预览部署

## 维护指南

### 更新应用信息

编辑 `src/apps-data.js` 文件：

```javascript
export const APPS = [
    {
        id: 'app-id',
        screenshot: {
            src: '/images/app-screenshot.webp',
            placeholder: 'data:image/svg+xml;base64,...',
            altKey: 'app.app_id.alt'
        },
        titleKey: 'app.app_id.title',
        descKey: 'app.app_id.description',
        featuresKey: 'app.app_id.features',
        genreKey: 'game.genre.type',
        appStoreUrl: 'https://...',
        googlePlayUrl: 'https://...',
        color: 'indigo',
        icon: 'fas fa-icon'
    },
    // ... 更多应用
];
```

### 添加新语言

1. 在 `src/locales/` 目录下创建新的语言文件，如 `es.json`
2. 在 `src/locales/index.js` 的 `SUPPORTED_LANGUAGES` 数组中添加语言代码
3. 确保所有翻译键都包含在新语言文件中

### 更新翻译

编辑相应的语言 JSON 文件：

```json
{
    "app.app_id.title": "应用标题",
    "app.app_id.description": "应用描述",
    "app.app_id.features": "• 特性1\n• 特性2"
}
```

### 添加新应用截图

1. 将图片放入 `public/images/` 目录
2. 建议使用 WebP 格式以获得最佳性能
3. 在 `apps-data.js` 中更新 `screenshot.src` 路径

## 性能优化

### 图片优化

- 使用 WebP 格式（比 JPEG 小 25-35%）
- 提供不同分辨率的图片
- 使用懒加载和占位图

### 代码优化

- Vite 自动代码分割
- Tailwind CSS JIT 模式，只生成使用的样式
- 异步加载语言包

### 网络优化

- Cloudflare 自动启用 HTTP/2 和 Brotli 压缩
- 长期缓存静态资源
- 预连接关键第三方域名

## 浏览器支持

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+
- 移动端 Chrome/Firefox/Safari 最新版本

## 许可证

GNU GPL v3 - 详见 LICENSE 文件